import React from 'react'
import {MenuItem} from './MenuItem'
import {MenuInnerWithSub} from './MenuInnerWithSub'
import { APP_URL, useAuth } from 'shared'

export function MenuInner() {
  const { currentUser } = useAuth()

  return (
    <>
      
      
    </>
  )
}
